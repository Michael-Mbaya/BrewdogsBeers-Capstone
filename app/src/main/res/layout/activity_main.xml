<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:textAlignment="center"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/brewIntroTop"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="#8C3636"
        android:contentDescription="brewdog_name"
        android:gravity="center"
        android:text="BrewDog's Beers"
        android:textColor="#FFFFFF"
        android:textSize="24sp"
        app:layout_constraintBottom_toTopOf="@+id/logoImage"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/logoImage"
        android:layout_width="409dp"
        android:layout_height="224dp"
        android:layout_marginTop="60dp"
        android:background="@color/blackLogo"
        android:contentDescription="brewdog_logo"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/brewdog_logo_2" />

    <EditText
        android:id="@+id/editText"
        android:layout_width="wrap_content"
        android:layout_height="42dp"
        android:layout_marginTop="172dp"
        android:contentDescription="abv_editText"
        android:drawableLeft="?android:attr/actionModeWebSearchDrawable"
        android:drawablePadding="5dp"
        android:gravity="center"
        android:hint="@string/what_should_we_call_you"
        android:maxLines="1"
        android:minLines="1"
        android:textCursorDrawable="@android:color/background_dark"
        android:textSize="18sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/logoImage"
        app:layout_constraintVertical_bias="0.0">

    </EditText>

    <Button
        android:id="@+id/nextActButton"
        android:layout_width="210dp"
        android:layout_height="57dp"
        android:background="#3F51B5"
        android:contentDescription="nextActivityButton"
        android:text="Find Beers"
        android:textColor="#FFFFFF"
        android:textSize="18sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.497"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/editText" />

</androidx.constraintlayout.widget.ConstraintLayout>